# netwerkinventory
# LIBRARIES
$ pip install netmiko pyats genie
# Create a folder
$ mkdir git_inventory
# Clone the netwerkinventory repo 
$ git clone https://github.com/saadaster/netwerkinventory.git
# Change into the new directory
$ cd git_inventory
